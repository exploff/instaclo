import { ClickOutsideMessageDirective } from './click-outside-message.directive';

describe('ClickOutsideMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickOutsideMessageDirective();
    expect(directive).toBeTruthy();
  });
});
